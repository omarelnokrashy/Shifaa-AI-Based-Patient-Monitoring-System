fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30

python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
openai==1.35.3
python-multipart==0.0.9
alembic==1.13.1
faker==25.0.0
httpx==0.27.0
websockets==12.0
pandas==2.2.2
gunicorn>=22.0.0

# ── AI/ML Core ───────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0.0
# NOTE: The commands below install the CPU-only torch build.
# For GPU inference (required for seizure detection & fall detection) install
# the CUDA-enabled build INSTEAD before running pip install -r requirements.txt:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# or for CUDA 12.x:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0
# NOTE: Install onnxruntime-gpu instead of onnxruntime for GPU acceleration:
#   pip install onnxruntime-gpu>=1.23.0
# The CPU build below is a safe default for the backend and arrhythmia service.
onnxruntime>=1.16.0
transformers>=4.38.0
timm>=0.9.12

# ── Microservice Specific (ECG, Fall & Seizure) ───────────────────────────────
opencv-python-headless>=4.8.0,<4.13.0
ultralytics>=8.0.0
mediapipe>=0.10.0
wfdb>=4.1.0
scikit-learn>=1.3.0
scipy>=1.11.0
h5py>=3.9.0

# ── Logging, Visualization & Utilities ────────────────────────────────────────
PyYAML>=6.0
tqdm>=4.65.0
matplotlib>=3.8.0
tensorboardX>=2.6.0
Pillow>=10.0.0
